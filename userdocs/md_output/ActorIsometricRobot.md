## Исполнитель "Вертун"

# Вертун

## Общие сведения

Исполнитель Вертун является основным исполнителем системы ПиктоМир, его
            наличие в КуМире необходимо для перехода от одной системы к другой.

Поле исполнителя - это прямоугольный участок на плоскости, ограниченный со
            всех сторон стенами и возможно, имеющий стены внутри.

Поле разделено на квадратные клетки, которые могут быть отмеченными точкой
            или закрашенными.

Исполнителю Вертун необходимо пройти по полю и закрасить те клетки, которые
            отмечены точкой, но не закрашены.

Вертун может перемещаться по полю, не натыкаясь на стены. Также он может
            закрашивать клетки или проверять их закрашенность (в некоторых задачах это
            требуется для определения местоположения Вертуна).

## Команды действий

## вперед

### Синтаксис (Кумир):
**Package:** Вертун

`алг вперед ()`**`вперед`**

### Синтаксис (Python):
**Package:** isometric_robot

`def go_forward ()`**`go_forward`**

Перемещает Вертуна на одну клетку вперед. Если перед
                Вертуном находится стена, то возникает ошибка выполнения "Робот ударился об
                стену".

## повернуть налево

### Синтаксис (Кумир):
**Package:** Вертун

`алг повернуть налево ()`**`повернуть налево`**

### Синтаксис (Python):
**Package:** isometric_robot

`def turn_left ()`**`turn_left`**

Поворачивает Вертуна на 90 градусов против часовой стрелки.

## повернуть направо

### Синтаксис (Кумир):
**Package:** Вертун

`алг повернуть направо ()`**`повернуть направо`**

### Синтаксис (Python):
**Package:** isometric_robot

`def turn_right ()`**`turn_right`**

Поворачивает Вертуна на 90 градусов по часовой стрелке.

## закрасить

### Синтаксис (Кумир):
**Package:** Вертун

`алг закрасить ()`**`закрасить`**

### Синтаксис (Python):
**Package:** isometric_robot

`def do_paint ()`**`do_paint`**

Закрашивает клетку, на которой находится Вертун.

## Команды опроса состояния

## впереди стена

### Синтаксис (Кумир):
**Package:** Вертун

`алг лог впереди стена ()`**`впереди стена`**

### Синтаксис (Python):
**Package:** isometric_robot

`def is_wall_ahead ()`**`is_wall_ahead`**

Возвращает

, если перед Вертуном находится
                стена, или
, если Вертун может спокойно пройти сквозь стену.
## впереди свободно

### Синтаксис (Кумир):
**Package:** Вертун

`алг лог впереди свободно ()`**`впереди свободно`**

### Синтаксис (Python):
**Package:** isometric_robot

`def is_free_ahead ()`**`is_free_ahead`**

Возвращает

,
                если Вертун может спокойно пройти сквозь стену,
                или
,
                если перед Вертуном находится стена.
## клетка закрашена

### Синтаксис (Кумир):
**Package:** Вертун

`алг лог клетка закрашена ()`**`клетка закрашена`**

### Синтаксис (Python):
**Package:** isometric_robot

`def is_cell_painted ()`**`is_cell_painted`**

Возвращает

,
                если клетка, на которой находится Вертун, закрашена,
                или
,
                если клетка чистая.
## клетка чистая

### Синтаксис (Кумир):
**Package:** Вертун

`алг лог клетка чистая ()`**`клетка чистая`**

### Синтаксис (Python):
**Package:** isometric_robot

`def is_cell_clean ()`**`is_cell_clean`**

Возвращает

,
                если клетка, на которой находится Вертун, чистая,
                или
,
                если клетка закрашена.
## Алгоритмы контроля обстановки исполнителя "Вертун"

Алгоритмы, описанные ниже могут быть вызваны только из "скрытой" части программы. Они служат для получении
			различных данных о текущей обстановке исполнителя

## Система координат Вертуна

Учительские алгоритмы предполагают получение информации о поле Вертуна с использованием
                        декартовой системы координат. Клетки нумеруются с единицы по двум координатам.

## @@есть точка

### Синтаксис (Кумир):
**Package:** Вертун

`алг лог@@есть точка (цел x, цел y)`**`@@есть точка`**

### Синтаксис (Python):
**Package:** isometric_robot

`def bool@@is_marked (int x, int y)`**`@@is_marked`**

Проверяет, есть ли точка в клетке с координатами

,
)
.
				Если точка в клетке есть, возвращает
, иначе --
.
## @@клетка закрашена

### Синтаксис (Кумир):
**Package:** Вертун

`алг лог@@клетка закрашена (цел x, цел y)`**`@@клетка закрашена`**

### Синтаксис (Python):
**Package:** isometric_robot

`def bool@@is_painted (int x, int y)`**`@@is_painted`**

Проверяет, закрашена ли клетка с координатами

,
)
.
				Если клетка закрашена, возвращает
, иначе --
.
## @@положение робота x

### Синтаксис (Кумир):
**Package:** Вертун

`алг цел@@положение робота x ()`**`@@положение робота x`**

### Синтаксис (Python):
**Package:** isometric_robot

`def int@@position_x ()`**`@@position_x`**

Возвращает

-координату текущего положения исполнителя "Вертун".
## @@положение робота y

### Синтаксис (Кумир):
**Package:** Вертун

`алг цел@@положение робота y ()`**`@@положение робота y`**

### Синтаксис (Python):
**Package:** isometric_robot

`def int@@position_y ()`**`@@position_y`**

Возвращает

-координату текущего положения исполнителя "Вертун".
## @@размер поля x

### Синтаксис (Кумир):
**Package:** Вертун

`алг цел@@размер поля x ()`**`@@размер поля x`**

### Синтаксис (Python):
**Package:** isometric_robot

`def int@@size_x ()`**`@@size_x`**

Возвращает ширину обстановки исполнителя "Вертун".

## @@размер поля y

### Синтаксис (Кумир):
**Package:** Вертун

`алг цел@@размер поля y ()`**`@@размер поля y`**

### Синтаксис (Python):
**Package:** isometric_robot

`def int@@size_y ()`**`@@size_y`**

Возвращает высоту обстановки исполнителя "Вертун".

## @@финишная

### Синтаксис (Кумир):
**Package:** Вертун

`алг лог@@финишная (цел x, цел y)`**`@@финишная`**

Проверяет, есть ли финишный флаг в клетке с координатами

,
)
.
                Если флаг в клетке есть, возвращает
, иначе --
.