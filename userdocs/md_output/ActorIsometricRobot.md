# Исполнитель "Вертун"

# Вертун

## Общие сведения

Исполнитель Вертун является основным исполнителем системы ПиктоМир, его
            наличие в КуМире необходимо для перехода от одной системы к другой.

Поле исполнителя - это прямоугольный участок на плоскости, ограниченный со
            всех сторон стенами и возможно, имеющий стены внутри.

Поле разделено на квадратные клетки, которые могут быть отмеченными точкой
            или закрашенными.

Исполнителю Вертун необходимо пройти по полю и закрасить те клетки, которые
            отмечены точкой, но не закрашены.

Вертун может перемещаться по полю, не натыкаясь на стены. Также он может
            закрашивать клетки или проверять их закрашенность (в некоторых задачах это
            требуется для определения местоположения Вертуна).

## Команды действий

### вперед

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**алг `вперед` ()**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**def `go_forward` ()**

Перемещает Вертуна на одну клетку вперед. Если перед
                Вертуном находится стена, то возникает ошибка выполнения "Робот ударился об
                стену".

### повернуть налево

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**алг `повернуть налево` ()**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**def `turn_left` ()**

Поворачивает Вертуна на 90 градусов против часовой стрелки.

### повернуть направо

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**алг `повернуть направо` ()**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**def `turn_right` ()**

Поворачивает Вертуна на 90 градусов по часовой стрелке.

### закрасить

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**алг `закрасить` ()**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**def `do_paint` ()**

Закрашивает клетку, на которой находится Вертун.

## Команды опроса состояния

### впереди стена

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**лог `впереди стена` ()**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**def `is_wall_ahead` ()**

Возвращает `да` `True` , если перед Вертуном находится
                стена, или `нет` `False` , если Вертун может спокойно пройти сквозь стену.

### впереди свободно

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**лог `впереди свободно` ()**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**def `is_free_ahead` ()**

Возвращает `да` `True` ,
                если Вертун может спокойно пройти сквозь стену,
                или `нет` `False` ,
                если перед Вертуном находится стена.

### клетка закрашена

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**лог `клетка закрашена` ()**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**def `is_cell_painted` ()**

Возвращает `да` `True` ,
                если клетка, на которой находится Вертун, закрашена,
                или `нет` `False` ,
                если клетка чистая.

### клетка чистая

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**лог `клетка чистая` ()**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**def `is_cell_clean` ()**

Возвращает `да` `True` ,
                если клетка, на которой находится Вертун, чистая,
                или `нет` `False` ,
                если клетка закрашена.

## Алгоритмы контроля обстановки исполнителя "Вертун"

Алгоритмы, описанные ниже могут быть вызваны только из "скрытой" части программы. Они служат для получении
различных данных о текущей обстановке исполнителя **Вертун** .

### Система координат Вертуна

Учительские алгоритмы предполагают получение информации о поле Вертуна с использованием
                        декартовой системы координат. Клетки нумеруются с единицы по двум координатам.

  Система координат Вертуна. Поле на этом рисунке имеет размеры: 6 клеток в ширину,
                        и 5 клеток в высоту

Система координат Вертуна. Поле на этом рисунке имеет размеры: 6 клеток в ширину,
                        и 5 клеток в высоту

### @@есть точка

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**лог `@@есть точка` (`x`, `y`)**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**bool `@@is_marked` (`x`, `y`)**

Проверяет, есть ли точка в клетке с координатами `( `x` , `y` )` .
Если точка в клетке есть, возвращает `да` , иначе - `нет` .

### @@клетка закрашена

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**лог `@@клетка закрашена` (`x`, `y`)**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**bool `@@is_painted` (`x`, `y`)**

Проверяет, закрашена ли клетка с координатами `( `x` , `y` )` .
Если клетка закрашена, возвращает `да` , иначе - `нет` .

### @@положение робота x

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**цел `@@положение робота x` ()**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**int `@@position_x` ()**

Возвращает `x` -координату текущего положения исполнителя "Вертун".

### @@положение робота y

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**цел `@@положение робота y` ()**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**int `@@position_y` ()**

Возвращает `y` -координату текущего положения исполнителя "Вертун".

### @@размер поля x

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**цел `@@размер поля x` ()**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**int `@@size_x` ()**

Возвращает ширину обстановки исполнителя "Вертун".

### @@размер поля y

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**цел `@@размер поля y` ()**

### Синтаксис (Python):

**Пакет:** isometric_robot

isometric_robot

**int `@@size_y` ()**

Возвращает высоту обстановки исполнителя "Вертун".

### @@финишная

### Синтаксис (Кумир):

**Пакет:** Вертун

Вертун

**лог `@@финишная` (`x`, `y`)**

Проверяет, есть ли финишный флаг в клетке с координатами `( `x` , `y` )` .
                Если флаг в клетке есть, возвращает `да` , иначе - `нет` .


## Неизвестные теги

| Тег | Количество | Атрибуты | Родительские теги |
|-----|------------|----------|-------------------|
| caption | 1 |  | mediaobject |
| imagedata | 2 | fileref='ActorIsometricRobot/coordinates.pdf', fileref='ActorIsometricRobot/coordinates.svg', format='PDF', format='SVG' | imageobject |
| imageobject | 2 |  | mediaobject |
| mediaobject | 1 |  | section |
| package | 29 |  | funcsynopsisinfo |