## Алгоритмы обработки строк

## Алгоритмы обработки строк

### верхний регистр

### верхний регистр

### Синтаксис (Кумир):

**Package:** Строки

**Package:** Строки

**алг лит `верний регистр`(лит строка)**

алг лит
верний регистр
лит
строка
Возвращает строку, все символы которой приведены к верхнему регистру символов.

### нижний регистр

### нижний регистр

### Синтаксис (Кумир):

**Package:** Строки

**Package:** Строки

**алг лит `нижний регистр`(лит строка)**

алг лит
нижний регистр
лит
строка
Возвращает строку, все символы которой приведены к нижнему регистру символов.

### позиция

### позиция

### Синтаксис (Кумир):

**Package:** Строки

**Package:** Строки

**алг цел `позиция`(лит строка, лит фрагмент)**

алг цел
позиция
лит
строка
лит
фрагмент
или сокращенный вариант:

### Синтаксис (Кумир):

**Package:** Строки

**Package:** Строки

**алг цел `поз`(лит строка, лит фрагмент)**

алг цел
поз
лит
строка
лит
фрагмент
Возвращает позицию первого символа подстроки`фрагмент`в строке`строка`. Если фрагмент не найден, то возвращает значение`0`.

фрагмент
в строке

строка
. Если фрагмент не найден, то возвращает значение

`0.`
.

### позиция после

### позиция после

### Синтаксис (Кумир):

**Package:** Строки

**Package:** Строки

**алг цел `позиция после`(цел начало, лит строка, лит фрагмент)**

алг цел
позиция после
цел
начало
лит
строка
лит
фрагмент
или сокращенный вариант:

### Синтаксис (Кумир):

**Package:** Строки

**Package:** Строки

**алг цел `поз после`(цел начало, лит строка, лит фрагмент)**

алг цел
поз после
цел
начало
лит
строка
лит
фрагмент
Возвращает позицию первого символа подстроки`фрагмент`в строке`строка`, при этом поиск подстроки начинается с позиции`начало`. Если фрагмент не найден, то возвращает значение`0`.

фрагмент
в строке

строка
, при этом поиск подстроки начинается с позиции

начало
. Если фрагмент не найден, то возвращает значение

`0.`
.

### вставить

### вставить

### Синтаксис (Кумир):

**Package:** Строки

**Package:** Строки

**алг `вставить`(лит фрагмент, аргрез лит строка, арг цел начало)**

алг
вставить
лит
фрагмент
аргрез лит
строка
арг цел
начало
В строку`строка`вставляет строку`фрагмент`,
            начиная с символа с номером`начало`.

строка
вставляет строку

фрагмент
,
            начиная с символа с номером

начало
.

Если``,
            то фрагмент добавляется в конец строки.

`начало=длин(строка)+1,
            то фрагмент добавляется в конец строки.`
начало
=

длин
(

строка
)+1

,
            то фрагмент добавляется в конец строки.

Если``или``,
            то возникает ошибка выполнения.

`начало< 1 или`
начало
< 1

или

`начало>длин(строка)+1,
            то возникает ошибка выполнения.`
начало
>

длин
(

строка
)+1

,
            то возникает ошибка выполнения.

### заменить

### заменить

### Синтаксис (Кумир):

**Package:** Строки

**Package:** Строки

**алг `заменить`(аргрез лит строка, арг лит старый фрагмент, арг лит новый фрагмент, арг лог каждый)**

алг
заменить
аргрез лит
строка
арг лит
старый фрагмент
арг лит
новый фрагмент
арг лог
каждый
Меняет в строке`строка`подстроку`старый фрагмент`на`новый фрагмент`.

строка
подстроку

старый фрагмент
на

новый фрагмент
.

Если``, то меняет все вхождения подстроки.

`каждый= да, то меняет все вхождения подстроки.`
каждый
= да

, то меняет все вхождения подстроки.

Если``,
            то меняет только первое вхождение подстроки.

`каждый= нет,
            то меняет только первое вхождение подстроки.`
каждый
= нет

,
            то меняет только первое вхождение подстроки.

### удалить

### удалить

### Синтаксис (Кумир):

**Package:** Строки

**Package:** Строки

**алг `удалить`(аргрез лит строка, арг цел начало, арг цел количество)**

алг
удалить
аргрез лит
строка
арг цел
начало
арг цел
количество
Удаляет`количество`символов из строки`строка`начиная с позиции`начало`.

количество
символов из строки

строка
начиная с позиции

начало
.

Если``,
            то удаляется текст до конца строки.

`начало+количество
            >длин(строка)+1,
            то удаляется текст до конца строки.`
начало
+

количество
>

длин
(

строка
)+1

,
            то удаляется текст до конца строки.

Если``или``,
            то возникает ошибка выполнения.

`начало< 1 или`
начало
< 1

или

`начало>длин(строка)+1,
            то возникает ошибка выполнения.`
начало
>

длин
(

строка
)+1

,
            то возникает ошибка выполнения.