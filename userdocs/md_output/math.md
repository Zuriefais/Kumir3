## Математика

## sqrt

### Синтаксис (Кумир):
`алг вещ sqrt (вещ x)`**`sqrt`**

Возвращает

-- квадратный корень из
Значение

.
### Пример
```кумир
вещ х
алг
нач
  ввод х
  х := sqrt (х)
  вывод "корень квадратный из числа х равен ", х
кон
```

## abs

### Синтаксис (Кумир):
`алг вещ abs (вещ x)`**`abs`**

Возвращает абсолютное (неотрицательное) значение вещественного числа

### Пример
```кумир
вещ а, б
алг
нач
  ввод а,б
  а := а+б
  а := abs(а)
  вывод "Модуль суммы чисел равен ", а
кон
```

## iabs

### Синтаксис (Кумир):
`алг цел iabs (цел x)`**`iabs`**

Возвращает абсолютное (неотрицательное) значение целого числа

### Пример
```кумир
цел а, б
алг
нач
  ввод а,б
  а := iabs(а)
  б := iabs(б)
  вывод а+б
кон
```

## sign

### Синтаксис (Кумир):
`алг цел sign (вещ x)`**`sign`**

Возвращает абсолютное знак числа

- ****



, если
- ****



, если
- ****



, если
### Пример
```кумир
цел а, б
алг
нач
  ввод а
  б := sign(а)
  если б=-1
    то вывод а, "<=0"
    иначе
      если б=0
        то вывод а, "=0"
        иначе вывод а, ">=0"
      все
  все
кон
```

## sin

### Синтаксис (Кумир):
`алг вещ sin (вещ x)`**`sin`**

Возвращает синус

### Пример
```кумир
вещ х
алг
нач
  ввод х
  х := sin (х)
  вывод "синус угла х равен ", х
кон
```

## cos

### Синтаксис (Кумир):
`алг вещ cos (вещ x)`**`cos`**

Возвращает косинус

### Пример
```кумир
вещ х
алг
нач
  ввод х
  х := cos (х)
  вывод "косинус угла х равен ", х
кон
```

### Пример
```кумир
вещ x, y
алг
нач
  вывод "угол x="
  ввод x
  y := 2*sin(x)*cos(x)
  вывод "sin2x = ", y
кон
```

## tg

### Синтаксис (Кумир):
`алг вещ tg (вещ x)`**`tg`**

Возвращает тангенс

### Пример
```кумир
вещ х
алг
нач
  ввод х
  х := tg (х)
  вывод "тангенс угла х равен ", х
кон
```

## ctg

### Синтаксис (Кумир):
`алг вещ ctg (вещ x)`**`ctg`**

Возвращает котангенс

### Пример
```кумир
вещ х
алг
нач
  ввод х
  х := ctg (х)
  вывод "котангенс угла х равен ", х
кон
```

## arcsin

### Синтаксис (Кумир):
`алг вещ arcsin (вещ x)`**`arcsin`**

Возвращает арксинус

### Пример
```кумир
вещ х
алг
нач
  ввод х
  х := arcsin (х)
  вывод "арксинус числа х равен ", х
кон
```

## arccos

### Синтаксис (Кумир):
`алг вещ arccos (вещ x)`**`arccos`**

Возвращает арккосинус

### Пример
```кумир
вещ х
алг
нач
  ввод х
  х := arccos (х)
  вывод "арккосинус числа х равен ", х
кон
```

## arctg

### Синтаксис (Кумир):
`алг вещ arctg (вещ x)`**`arctg`**

Возвращает арктангенс

### Пример
```кумир
вещ х
алг
нач
  ввод х
  х := arctg (х)
  вывод "арктангенс числа х равен ", х
кон
```

## arcctg

### Синтаксис (Кумир):
`алг вещ arcctg (вещ x)`**`arcctg`**

Возвращает арккотангенс

### Пример
```кумир
вещ х
алг
нач
  ввод х
  х := arcctg (х)
  вывод "арккотангенс числа х равен ", х
кон
```

## ln

### Синтаксис (Кумир):
`алг вещ ln (вещ x)`**`ln`**

Возвращает натуральный логарифм

### Пример
```кумир
вещ а,б,с
алг
нач
  ввод а, б
  с := а+б
  с := ln(с)
  вывод "Натуральный логарифм от суммы чисел ",а," и ",б," равен ",с
кон
```

## lg

### Синтаксис (Кумир):
`алг вещ lg (вещ x)`**`lg`**

Возвращает десятичный логарифм

### Пример
```кумир
вещ а,б,с
алг
нач
  ввод а, б
  с := а+б
  с := lg(с)
  вывод "Десятичный логарифм от суммы чисел ",а," и ",б," равен ",с
кон
```

## exp

### Синтаксис (Кумир):
`алг вещ exp (вещ x)`**`exp`**

Возвращает

### Пример
```кумир
вещ x
цел а
алг
нач
  ввод а
  x := exp(а)
  вывод "число е в степени ", а, " равно ", x
кон
```

## min

### Синтаксис (Кумир):
`алг вещ min (вещ x, вещ y)`**`min`**

Возвращает наименьшее из вещественных значений

## max

### Синтаксис (Кумир):
`алг вещ max (вещ x, вещ y)`**`max`**

Возвращает наибольшее из вещественных значений

### Пример
```кумир
вещ а,б,с1, с2
алг
нач
  ввод а, б
  с1 := max(а,б)
  с2 := min(а,б)
  вывод с1, нс
  вывод с2, нс
кон
```

## imin

### Синтаксис (Кумир):
`алг цел imin (цел x, цел y)`**`imin`**

Возвращает наименьшее из целых значений

## imax

### Синтаксис (Кумир):
`алг цел imax (цел x, цел y)`**`imax`**

Возвращает наибольшее из целых значений

### Пример
```кумир
цел а,б,с1, с2
алг
нач
  ввод а, б
  с1 := imax(а,б)
  с2 := imin(а,б)
  вывод с1, нс
  вывод с2, нс
кон
```

## div

### Синтаксис (Кумир):
`алг цел div (цел x, цел y)`**`div`**

Возвращает частное от деления целых значений

## mod

### Синтаксис (Кумир):
`алг цел mod (цел x, цел y)`**`mod`**

Возвращает остаток от деления целых значений

### Пример
```кумир
цел а,б,с1, с2
алг
нач
  ввод а, б
  с1 := imax(а,б)
  с2 := imin(а,б)
  вывод с1, нс
  вывод с2, нс
кон
```

## int

### Синтаксис (Кумир):
`алг цел int (вещ x)`**`int`**

Возвращает целую часть числа

### Пример
```кумир
вещ а, б
алг
нач
  ввод а
  б := int(а)
  вывод "Целая часть ", а, " равна ", б
кон
```

## rnd

### Синтаксис (Кумир):
`алг вещ rnd (вещ x)`**`rnd`**

Случайное число из интервала от 0 до

### Пример
```кумир
алг Построение последовательности случайных вещественных чисел
нач
  вещ таб а [1:10]
  цел л
  вещ б
  ввод б
  нц для л от 1 до 10
    а[л] := rnd(б)
  кц
  нц для л от 1 до 10
    вывод а[л], " "
  кц
кон
```

## rand

### Синтаксис (Кумир):
`алг вещ rand (вещ x, вещ y)`**`rand`**

Случайное число из интервала от

## irnd

### Синтаксис (Кумир):
`алг цел irnd (цел x)`**`irnd`**

Случайное целое число из интервала от 0 до

## irand

### Синтаксис (Кумир):
`алг цел irnd (цел x, цел y)`**`irnd`**

Случайное целое число из интервала от

## МАКСЦЕЛ

### Синтаксис (Кумир):
`алг цел МАКСЦЕЛ ()`**`МАКСЦЕЛ`**

Самое большое целое число, которое можно использовать в языке Кумир:

.
            Величина этого числа определяется способом представления чисел в современных
            компьютерах и примерно одинаковая в большинстве современных языков программирования.
## МАКСВЕЩ

### Синтаксис (Кумир):
`алг вещ МАКСВЕЩ ()`**`МАКСВЕЩ`**

Самое большое вещественное число, которое можно использовать в языке Кумир: значение, несколько
            меньшее чем

.